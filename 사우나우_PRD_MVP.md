# 사우나우(SAUNAU) — MVP PRD

> Claude Code 개발 착수용 요구사항 정의서. 화면 디자인은 별도 문서 `사우나우_화면_프롬프트팩.md`(Claude Design 핸드오프)를 참조.
> 본 PRD는 첨부 요구사항을 기반으로 하되, 더 나은 데이터 처리·아키텍처를 일부 반영했다(문서 끝 **개선 노트** 참조). 한정판 굿즈(타임 키링) 로직은 범위에서 제외.

---

## 1. 개요

**한 줄 정의**: 전국의 사우나·대중탕·온천·찜질방을 깔끔한 UI로 탐색하고, 사우나실/냉탕 온도 같은 마니아 정보까지 보여주는 디스커버리 서비스.

**타겟 유저**: 사우나를 즐기는 2030(MZ). "내 주변 갈 만한 쾌적한 사우나"를 빠르게 찾고 싶지만, 네이버 지도는 사진·정보가 빈약하고 숨은 좋은 곳은 안 보인다.

**차별점(wedge)**:
- 네이버에 없는 **구조화된 사우나 정보** — 사우나실/냉탕 온도, 세신·찜질방·온천 여부, 시설 스펙.
- **에디터 큐레이션 + 읽을거리(효능·소식·가이드)** — 취향 기반 발견.
- 합법 공공데이터(목욕장업) 기반의 **전국 커버리지**.

**플랫폼**: 모바일 웹앱(반응형). 데스크톱 접속 시 화면 중앙에 모바일 규격(max-w-[430px]) 뷰포트 프레임 고정 + 외곽 배경 분리.

---

## 2. 목표 / 비목표

### 목표
- 출시 시점에 주요 도시 핵심 사우나가 **빈 정보 없이** 보인다(특히 온도 헤드라인이 채워져 있다).
- 사용자가 **3탭 이내**로 "내 주변 갈 만한 사우나"를 찾는다.
- 각 사우나 상세 페이지가 **검색 색인**되어 유기 유입(SEO)이 발생한다.
- 가벼운 **다녀옴(체크인)**으로 데이터가 살아 움직이고, 재방문 동기가 생긴다.

### 비목표 (이번 버전에서 안 함)
- **자유 텍스트 리뷰·사진 업로드 UGC** — 모더레이션 부담. 다녀옴(객관식)만.
- **회원가입/로그인** — 디바이스 식별(익명)으로 충분. 계정은 동기화가 필요해질 때.
- **결제·티켓·예약** — 운영자 온보딩 부담. demand 검증 후.
- **한정판 굿즈 챌린지** — 범위 제외.
- **실시간 혼잡도, 정밀 운영여부** — 군중 형성 후(v2).
- **자체 지도 타일** — 네이버/카카오 SDK 사용. 자산은 POI·후기 DB이지 베이스맵이 아님.

---

## 3. MVP 범위 요약

**포함(P0)**: 공공데이터 초기 적재 + 정제, 동기화 Cron, 홈(디스커버리·큐레이션·매거진), 사우나 목록/지도/검색/필터, 상세, 찜하기(하트), 다녀옴(3초 체크인), 새로 오픈, SEO 기본.
**P1**: 매거진 다건, 다녀옴 태그 기반 동적 보정(고온/노천 가중치), 지역 인덱스 페이지 확장.
**P2(설계만)**: 온도 관측 평균/freshness 고도화, 회원 기반 마이페이지·통계(사 활), 운영자 클레임/광고.

---

## 4. 사용자 스토리 (핵심)

- 탐색자: "내 주변에서 사우나실 온도가 높고 세신 되는 곳을 빠르게 찾고 싶다."
- 마니아: "이 사우나의 사우나실/냉탕 온도, 습식/건식, 찜질방 여부를 한눈에 보고 싶다."
- 발견자: "안 가본 핫한/숲속 노천 사우나를 큐레이션으로 둘러보고 싶다."
- 학습자: "사우나 효능이나 올바른 이용법 같은 읽을거리를 보고 싶다."
- 기록자: "다녀온 곳을 3초 만에 가볍게 남기고, 마음에 든 곳은 찜해두고 싶다."

---

## 5. 정보구조 & 화면

**하단 탭(3)**: 홈 / 검색 / 찜

| 화면 | 역할 | 비고 |
|---|---|---|
| 홈(디스커버리) | 랜딩. 검색바 + 카테고리 빠른 진입 + 큐레이션 레일 + 읽을거리(매거진) + 새로 오픈 | 목록은 여기서 진입 |
| 목록 화면 | 내 주변/카테고리별 사우나 리스트 (지도 토글) | 타입 세그먼트 + 속성 필터 |
| 지도 화면 | 핀(대표 온도 라벨) + 하단 시트 | 목록과 토글 |
| 검색 | 상호/지역 검색, 최근/추천 | |
| 필터 시트 | 타입·시설유형·세신·영업·온도범위 | |
| 상세 | 사진 + 온도 헤드라인 + 시설 스펙 + 에디터 소개 + 미니맵 + 찜/다녀옴 | |
| 찜 | 찜한 사우나(로컬 저장) | |

> 화면별 레이아웃·구성은 `사우나우_화면_프롬프트팩.md` 참조. 디자인 시스템(흰 배경, Pretendard, 빨강 액센트, 플랫 아이콘, 이모지 금지, 여백 우선)도 동일 문서.

---

## 6. 데이터 모델 (Supabase / PostgreSQL + PostGIS)

### `saunas` (핵심 엔티티)

| 필드 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | |
| license_no | text (unique) | 인허가번호 — 동기화 upsert 키 |
| name | text | 상호명 |
| address | text | 소재지 |
| sido / sigungu / dong | text | 지역(인덱스·필터·SEO용 분해) |
| location | geography(Point,4326) | **WGS84 좌표** (아래 개선①) |
| status | text | 영업/정상만 노출 |
| phone | text | LOCALDATA 전화 |
| open_date | date | 개업일자(새로 오픈 정렬) |
| created_at | timestamptz | 시스템 등록일 |
| is_jjimjilbang | bool | 찜질방/불가마/한증막/스파 |
| is_hot_spring | bool | 온천 |
| is_24h | bool | 24시/연중무휴 |
| has_outdoor | bool | 노천 — **에디터 시딩**(공공데이터 없음) |
| sauna_room_temp | int (nullable) | **사우나실 온도** — 에디터 시딩(시그니처) |
| cold_bath_temp | int (nullable) | **냉탕 온도** — 에디터 시딩 |
| has_sesin | bool | 세신 — 에디터 시딩 |
| sauna_kind | text[] | 습식/건식/한증막 등 — 에디터 시딩 |
| price | int (nullable) | 입욕료 — 에디터 시딩 |
| hours | text (nullable) | 영업시간 텍스트 |
| thumbnail_url | text | 대표 사진 |
| editor_note | text | 에디터 한 줄/소개 |
| slug | text (unique) | SEO URL용 |

> 타입 분류: 모든 행은 기본적으로 **사우나·대중탕**. `is_jjimjilbang`/`is_hot_spring`는 추가 플래그(복수 가능). 홈 카테고리 "목욕탕"은 두 플래그가 모두 false인 순수 대중탕.
> 온도는 v1에서 2개 컬럼으로 단순화하되, 향후 시설별 온도(온탕/열탕 등)로 확장 가능하도록 `sauna_facilities` 분리를 P2로 둔다.

### `visits` (다녀옴 체크인)

| 필드 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | |
| sauna_id | uuid (FK) | |
| device_id | text | 익명 디바이스 식별(로그인 없음) |
| satisfaction | text | 개운해요/평범해요/아쉬워요 |
| tags | text[] | 객관식 키워드(시설 깨끗/온도 딱좋음/노천 좋음 등) |
| reported_sauna_temp | int (nullable) | **온도 제보(선택)** → 온도 보정에 사용 |
| reported_cold_temp | int (nullable) | |
| created_at | timestamptz | |

> 용도: ① 인기/방문 수, ② 태그 누적 → 동적 보정(고온/노천 가중치), ③ 온도 관측치 → 표시 온도 평균/freshness 보정.

### `collections` (큐레이션 레일)
`id, title, description, slug, sort, is_published` + 조인 테이블 `collection_saunas(collection_id, sauna_id, sort)`. 에디터 관리.

### `articles` (읽을거리/매거진)
`id, title, summary, body(markdown), thumbnail_url, category(효능/소식/가이드), slug, published_at, is_published`. SEO 대상.

---

## 7. 데이터 파이프라인

### [A] 초기 Bulk 적재 (1회, 스크립트)
1. 공공데이터포털/지방행정인허가데이터 **목욕장업** 전국 CSV 다운로드.
2. **정제**:
   - 영업상태가 영업/정상인 행만. 폐업·휴업 제외.
   - 상호명에 피트니스/골프/스포츠/클럽 등 → 헬스장 부속 샤워시설로 보고 1차 제거(키워드).
3. **좌표 변환(개선①)**: CSV의 `좌표정보(X/Y)`는 WGS84가 아니라 **TM 좌표(EPSG:5174)**다. `pyproj`로 5174 → 4326 재투영하여 `location`에 적재. 좌표가 비어 있는 행만 주소 기반 지오코딩(카카오/네이버)으로 폴백. → 외부 지오코딩 호출 최소화, 비용↓.
4. **메타데이터 매핑**(키워드):
   - `is_jjimjilbang` = 찜질방/불가마/한증막/스파 포함
   - `is_hot_spring` = 온천 포함 (개선②: v1 프록시. 정확 ground-truth는 온천법 공공데이터 교차검증을 P1/P2로)
   - `is_24h` = 영업시간/설명에 24시·연중무휴·00:00~24:00
   - `sauna_room_temp/cold_bath_temp/has_sesin/has_outdoor/sauna_kind/price/thumbnail/editor_note` = **에디터 시딩**(공공데이터에 없음). 상위 큐레이션 사우나부터 채운다.
5. `slug` 생성(지역+상호 기반, 중복 처리).
6. Supabase Bulk Insert.

### [B] 동기화 Cron (신규 오픈 추적)
- Vercel Cron이 주기적으로 지방행정인허가 공공 API 호출.
- **Upsert(키 = license_no)**: 존재하면 `status`만 최신화. 신규 license_no면 `open_date`/`created_at` 마킹 후 신규 row 추가(메타데이터는 키워드 기본값, 온도 등은 null → 에디터 보강 대기).
- "새로 오픈" = `open_date` 최신순 상위 N.

---

## 8. 핵심 기능 요구사항

### 8.1 위치 기반 탐색 (P0)
- 브라우저 Geolocation으로 좌표 획득 → PostGIS `ST_DWithin`/거리 계산으로 반경 내 사우나를 **거리순** 쿼리.
- 위치 거부 시: 기본 지역(예: 강남구) 폴백 + 지역 선택 UI.
- **수용기준**: 위치 허용 시 목록/지도가 가까운 순으로 채워진다 / 거부 시에도 빈 화면 없이 기본 지역이 보인다.

### 8.2 카테고리 & 필터 (P0)
- 홈 카테고리 카드(목욕탕/찜질방/온천/내 주변/핫플) 또는 목록 상단 타입 세그먼트(전체/온천/사우나/찜질방) 클릭 → 해당 플래그 true만 바인딩.
- 속성 필터: 세신/습식/24시간/온탕(온도)/노천. 다중 선택.
- **온도 필터(개선③)**: "고온(90°↑)"은 visit 누적이 아니라 **에디터 시딩된 `sauna_room_temp`에서 즉시 파생**. 노천도 에디터 `has_outdoor`로 즉시 동작. → 출시 시점 콜드스타트 없음. visit 태그는 시간이 지나며 **보정·가중치**로만 추가 작용(동적 조인은 P1).

### 8.3 온도 헤드라인 — 시그니처 (P0, 개선④)
- 목록 카드·상세 히어로에 **사우나실 온도(빨강) · 냉탕 온도(파랑)** 를 큰 tabular-nums로 노출.
- 값 출처/신선도 표시: 에디터 시딩값 기본. visit 온도 제보가 쌓이면 평균 + "N명 확인 · N주 전"(P1).
- **수용기준**: 큐레이션 사우나는 온도 헤드라인이 비어 있지 않다 / 온도 없는 롱테일은 "온도 정보 없음"의 우아한 빈 상태로 표시(깨짐 금지).

### 8.4 찜하기 (P0)
- 카드·상세 우상단 하트. 찜 = 빨강 꽉 찬 하트. 로그인 없이 **로컬/디바이스 저장**. 찜 탭에서 모아보기.

### 8.5 다녀옴(3초 체크인) (P0)
- 상세의 [다녀옴] → 객관식 바텀시트: 만족도(개운/평범/아쉬움) + 키워드 태그 + (선택) 온도 제보.
- 로그인 없이 device_id로 `visits` 적재.
- **어뷰징 가드(P1)**: 동일 device·sauna 단시간 중복 제출 제한.
- **수용기준**: 제출이 3초 내 완료된다 / 제출 후 해당 사우나의 방문 수·태그가 반영된다.

### 8.6 홈 큐레이션 & 매거진 (P0, 개선⑥)
- 큐레이션 레일(가로 스크롤) — `collections` 기반. v1 1~2개.
- 읽을거리(매거진) — `articles`(효능/소식/가이드). v1 3~4건.
- 새로 오픈 — `open_date` 최신순.
- **수용기준**: 레일/매거진이 비면 섹션 자체를 숨겨 휑하지 않게 한다.

---

## 9. SEO 요구사항 (P0, 개선⑤)
- **사우나 상세** = SSR/SSG, 크롤 가능한 고유 URL(`/sauna/[sido]/[slug]`).
- 상세에 schema.org **LocalBusiness** 구조화 데이터(이름·주소·좌표·전화·영업시간).
- **지역 인덱스 페이지**(`/area/[sido]/[sigungu]`)로 롱테일 검색 대응.
- 매거진 글도 색인 대상. sitemap.xml 생성.

---

## 10. 기술 스택 & 인프라
- **Frontend/Routing**: Next.js (App Router) — 모바일 웹앱, SSR/SSG로 SEO.
- **DB/Backend**: Supabase (PostgreSQL + PostGIS, RLS).
- **Deploy/Cron**: Vercel (GitHub 연동, Cron으로 동기화 배치).
- **Map**: 네이버 또는 카카오맵 웹 SDK 임베드.
- **데스크톱 래퍼**: 중앙 max-w-[430px] 모바일 프레임 + 외곽 배경 분리.

---

## 11. 개선 노트 (첨부 요구사항 대비 변경 & 이유)
1. **좌표**: "PostGIS/위경도 계산"에 앞서, LOCALDATA 좌표는 **TM(EPSG:5174)** 이므로 4326 재투영을 명시. 지오코딩은 좌표 결측 폴백으로만 → 정확도↑·비용↓.
2. **온천 판별**: 키워드 매칭은 v1 프록시로 두되, 온천은 온천법 별도 데이터라 교차검증을 후속 과제로 명시(오탐/누락 인지).
3. **고온/노천 필터**: visit 누적 의존(콜드스타트로 출시 시 빈 필터) → **에디터 시딩/온도 파생으로 즉시 동작**. visit은 보정 레이어로 강등.
4. **온도 1급화**: 첨부엔 온도가 동적 필터로만 약하게 존재 → 제품의 시그니처인 **사우나실/냉탕 온도 헤드라인**을 핵심 데이터·UI로 승격(에디터 시딩 + visit 제보 보정).
5. **SEO 아키텍처**: "SEO 대응" 추상 → 상세 URL·구조화 데이터·지역 인덱스·sitemap으로 구체화(유기 유입이 핵심 GTM).
6. **홈에 큐레이션·매거진 추가**: 단순 카테고리/리스트 홈 → 디스커버리(레일+읽을거리) 홈. 취향·콘텐츠가 차별점이자 SEO 자산.
7. **찜하기 + 다녀옴 분리**: 사전 의도(찜/하트)와 사후 기록(다녀옴/체크인)을 별개 액션으로 명확화.
8. **굿즈 챌린지 제외** + 그에 딸린 로그인/배송 요구 제거 → 다녀옴을 **무로그인(device_id)** 으로 가볍게 유지.

---

## 12. 오픈 퀘스천
- [데이터] 목욕장업 CSV에 영업시간·전화의 결측률은? (24h 판별·표시 영향) — *엔지니어/데이터*
- [지도] 네이버 vs 카카오 SDK 중 택1 (약관·쿼터·길찾기 연동) — *엔지니어*
- [콘텐츠] 출시 시 시딩할 큐레이션 사우나 수와 매거진 초기 건수 — *기획/에디터*
- [어뷰징] 다녀옴 device_id 위·변조 허용 범위 — *엔지니어*

---

## 13. 향후 (v2+)
- 온도 관측 평균·freshness 고도화, 시설별 온도(온탕/열탕) 분리.
- 회원 기반 사 활(체크인 기록·통계·연말 결산), 사진/리뷰 UGC + 모더레이션.
- 운영자 클레임·노출 광고, (검증 후) 회수권 디지털 판매.
- 온천법 데이터 교차검증, 외국인(영문) 라인.
